language:
  type: select
  comment: Language for syntax highlighting — the sample keeps its own colours rather than going flat like a `diff` fence. An unlisted language renders as plain text rather than failing the build.
  options:
    values:
      - id: astro
        name: astro
      - id: bash
        name: bash
      - id: css
        name: css
      - id: html
        name: html
      - id: js
        name: js
      - id: json
        name: json
      - id: jsx
        name: jsx
      - id: markdown
        name: markdown
      - id: mdx
        name: mdx
      - id: python
        name: python
      - id: ruby
        name: ruby
      - id: sh
        name: sh
      - id: ts
        name: ts
      - id: tsx
        name: tsx
      - id: yaml
        name: yaml
      - id: text
        name: text
code:
  type: code
  comment: The sample itself, exactly as it should read after the change. Indentation is preserved exactly as typed.
  options:
    max_visible_lines: 20
added:
  type: text
  comment: 'Lines added, 1-based: "3" or "3,7-9". Empty marks nothing.'
removed:
  type: text
  comment: 'Lines removed, 1-based: "2" or "2,5-6". Empty marks nothing.'
highlight:
  type: text
  comment: 'Lines to highlight, 1-based: "2" or "1,4-6". Empty highlights nothing.'
filename:
  type: text
  comment: Shown in the header bar. Falls back to the language when empty — a real path tells the reader which file to change.
showCopy:
  type: switch
  comment: Show the copy button.

# Inlined rather than shared: a second `_inputs_from_glob` entry on a structure
# value never reached the editor, leaving this field as free text.
spaceBefore:
  comment: Space above this block. Default follows the block's type; a Spacer replaces the gap entirely.
  type: select
  options:
    values:
      - id: default
        name: Default
      - id: none
        name: None
      - id: tight
        name: Tight
      - id: loose
        name: Loose
