## What changed

One paragraph. What a reader of the site would notice.

## Why

The problem this solves. Link an issue if there is one.

## Checklist

- [ ] `npm run check` passes
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`, or this change is internal-only
- [ ] Component props renamed? `.astro`, `inputs.yml`, `structure-value.yml`, `snippets.yml` and `src/content/` are all updated
- [ ] Checked in the CloudCannon editor, not only `npm run dev`
