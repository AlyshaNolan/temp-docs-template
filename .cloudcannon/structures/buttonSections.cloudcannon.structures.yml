buttonSections:
  id_key: _component
  style: modal
  values_from_glob:
    - /src/components/building-blocks/core-elements/button/button.cloudcannon.structure-value.yml
    # A "run this to start" command belongs in the same row as the primary
    # button, so it is offered wherever buttons are.
    - /src/components/building-blocks/core-elements/command-line/command-line.cloudcannon.structure-value.yml
