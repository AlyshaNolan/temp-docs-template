# Component catalog

Every component the library ships. Each is a directory under `src/components/`; its `_component` path is that directory path in kebab-case. This is the canonical list of what exists and when to use each — other skills link here.

**Full prop list for any entry:** open its `.astro` file and read the `Astro.props` destructure. The file path is mechanical — the last path segment PascalCased: `page-sections/heroes/hero-center` → `src/components/page-sections/heroes/hero-center/HeroCenter.astro`. For page sections, the editor-facing defaults and input types also live in the sibling `<slug>.cloudcannon.structure-value.yml` + `.inputs.yml`. The tables below list the **content** props an author sets — not every prop.

## Page sections

Place these directly in a page's `pageSections` array. **All page sections also take the standard shell props** (`sectionLabel`, `maxContentWidth`, `paddingHorizontal`, `paddingVertical`, `colorScheme`, `backgroundColor`, `background`) — see [SKILL.md → Standard section props](SKILL.md#standard-section-props-every-page-section). Only content props are listed here.

<!-- generated:catalog:page-sections:start (npm run docs:catalog) -->

### Heroes

| `_component`                       | Use for                                                     | Key content props                                                                                                                                                               |
| ---------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/heroes/hero-center` | Centered hero with a heading, supporting text, and buttons. | `eyebrow`, `eyebrowShowDot`, `heading`, `headingHighlight`, `headingLevel` (`h1`/`h2`/`h3`), `subtext` (markdown), `alignmentHorizontal` (`center`/`start`), `buttonSections[]` |

### Builders

| `_component`                            | Use for                                      | Key content props                       |
| --------------------------------------- | -------------------------------------------- | --------------------------------------- |
| `page-sections/builders/custom-section` | Groups and styles a large section of a page. | `label`, `contentSections[]`, `rounded` |

### Collections

| `_component`                                | Use for                                                                  | Key content props                                                                                                                                                                                                                                                                                  |
| ------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/collections/card-collection` | Grid of linked cards for services, case studies, projects, or resources. | `eyebrow`, `heading`, `headingLevel` (`h1`/`h2`/`h3`), `alignmentHorizontal` (`center`/`start`), `subtext` (markdown), `items[]`, `layout` (`grid`/`masonry`), `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `aspectRatio` (`landscape`/`widescreen`/`square`/`portrait`/`none`) |
| `page-sections/collections/feature-list`    | Compact grid of short feature labels with a line of detail each.         | `eyebrow`, `heading`, `headingLevel` (`h1`/`h2`/`h3`), `subtext` (markdown), `alignmentHorizontal` (`start`/`center`), `items[]`, `columns`, `gap` (`sm`/`md`/`lg`), `rounded`                                                                                                                     |

### Conversion

| `_component`                         | Use for                                                        | Key content props                                                                                                          |
| ------------------------------------ | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/conversion/cta-split` | Call to action with text on one side and buttons on the other. | `heading`, `headingLevel` (`h1`/`h2`/`h3`), `subtext` (markdown), `imageSource`, `imageAlt`, `buttonSections[]`, `reverse` |

### Explainers

| `_component`                              | Use for                                                  | Key content props                                                                                                                                                                                   |
| ----------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/explainers/faq-section`    | Frequently asked questions in an accordion.              | `heading`, `headingLevel` (`h1`/`h2`/`h3`), `items[]`                                                                                                                                               |
| `page-sections/explainers/stats`          | Row of big numbers with labels.                          | `eyebrow`, `heading`, `headingLevel` (`h1`/`h2`/`h3`), `alignmentHorizontal` (`center`/`start`), `subtext` (markdown), `stats[]`, `layout` (`tiles`/`rule`), `dividers`                             |
| `page-sections/explainers/steps`          | Numbered how-it-works sequence with optional images.     | `eyebrow`, `heading`, `headingLevel` (`h1`/`h2`/`h3`), `subtext` (markdown), `items[]`, `orientation` (`horizontal`/`vertical`), `imageAspectRatio` (`landscape`/`portrait`/`square`/`widescreen`)  |
| `page-sections/explainers/tabbed-content` | Tabbed panels for showing several things in one section. | `eyebrow`, `heading`, `headingLevel` (`h1`/`h2`/`h3`), `subtext` (markdown), `alignmentHorizontal` (`start`/`center`), `items[]`, `variant` (`plain`/`panel`), `navigationPosition` (`start`/`top`) |

<!-- generated:catalog:page-sections:end -->

## Building blocks

Composed **inside** page sections (in `contentSections`, `buttonSections`, `formBlocks`, `items`, `slides`, etc.), not placed directly in `pageSections`. Path prefix omitted in the tables below — prepend `building-blocks/`.

**Choosing a layout wrapper.** The containers under Wrappers overlap; pick by what the children _are_:

- `grid` — many items of the same kind. You give it an item width (or a column count) and it decides how many fit per row.
- `split` — exactly two columns, each holding its own blocks.
- `card-grid` — as `grid`, but every item is a card with a cover image.
- `masonry` — items of uneven height that should pack tightly instead of lining up in rows.

Reach for `grid` when the children repeat and are uniform, `split` when there are two of them and they differ.

<!-- generated:catalog:building-blocks:start (npm run docs:catalog) -->

### Core elements — `building-blocks/core-elements/<slug>`

| `<slug>`           | Use for                                                                                                | Key content props                                                                                                                                                                                                                                                                                    |
| ------------------ | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `alert`            | Aside for something a reader would otherwise learn the hard way.                                       | `variant` (`note`/`tip`/`warning`/`danger`/`info`), `title`, `text` (markdown), `iconName`                                                                                                                                                                                                           |
| `badge`            | Small pill label for statuses, tags, and announcements.                                                | `text`, `iconName`, `showDot`, `link`, `showArrow`, `variant` (`neutral`/`brand`/`info`/`success`/`warning`/`danger`/`plain`/`accent`), `size` (`sm`/`md`/`lg`), `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                               |
| `button`           | Clickable button for actions and links.                                                                | `text`, `hideText`, `link`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`), `variant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `size` (`sm`/`md`/`lg`)                                               |
| `changelog`        | Every release in the changelog collection, newest first.                                               | `limit`                                                                                                                                                                                                                                                                                              |
| `code-annotations` | Code with numbered notes underneath, so the snippet stays copy-paste clean.                            | `code`, `language`, `filename`, `highlight`, `notes[]`, `showCopy`                                                                                                                                                                                                                                   |
| `code-block`       | A syntax-highlighted code sample with a filename bar and a copy button.                                | `code`, `language`, `filename`, `highlight`, `showCopy`                                                                                                                                                                                                                                              |
| `code-diff`        | A code sample with added, removed and highlighted lines, still syntax-highlighted in its own language. | `code`, `language`, `filename`, `showCopy`                                                                                                                                                                                                                                                           |
| `code-tabs`        | One sample in several languages. The reader's choice is remembered across pages.                       | `tabs[]`, `filename`, `showCopy`                                                                                                                                                                                                                                                                     |
| `command-line`     | A single copyable shell command, sized to sit beside a button.                                         | `command`, `prompt`, `showCopy`                                                                                                                                                                                                                                                                      |
| `counter`          | Number that counts to a target value.                                                                  | `number`, `prefix`, `suffix`, `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                    |
| `definition-list`  | List of terms and their definitions.                                                                   | `items[]`, `layout` (`stacked`/`grid`), `alignmentHorizontal` (`start`/`center`/`end`)                                                                                                                                                                                                               |
| `diagram`          | A Mermaid diagram, written as text so it diffs in review.                                              | `definition`, `caption`                                                                                                                                                                                                                                                                              |
| `embed`            | Embed for maps, iframes, or raw HTML.                                                                  | `html`, `aspectRatio` (`square`/`landscape`/`portrait`/`widescreen`)                                                                                                                                                                                                                                 |
| `figure`           | A captioned screenshot, with a dark-mode variant and click-to-zoom.                                    | `source`, `alternateSource`, `alt`, `caption`, `zoom`, `aspectRatio` (`none`/`square`/`landscape`/`portrait`/`widescreen`)                                                                                                                                                                           |
| `file-tree`        | A directory tree drawn from an indented list of paths.                                                 | `paths`, `title`, `showCopy`                                                                                                                                                                                                                                                                         |
| `heading`          | Heading from h1 to h6.                                                                                 | `text`, `level` (`h1`/`h2`/`h3`/`h4`/`h5`/`h6`), `size` (`default`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`), `alignmentHorizontal` (`start`/`center`/`end`), `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`) |
| `icon`             | Icon from the icon library.                                                                            | `name`, `size` (`none`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `color` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                                    |
| `image`            | Image from a local file or a URL.                                                                      | `source`, `alternateSource`, `alt`, `decorative`, `rounded`, `aspectRatio` (`none`/`square`/`landscape`/`portrait`/`widescreen`/`horizontal-strip`), `positionVertical` (`top`/`center`/`bottom`), `positionHorizontal` (`left`/`center`/`right`), `priority`                                        |
| `param-list`       | API reference entries: name, type, whether it is required, and what it does.                           | `params[]`                                                                                                                                                                                                                                                                                           |
| `print-button`     | Opens the reader's print dialog. Hidden when JavaScript is off, since nothing else can open it.        | `text`, `variant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `size` (`sm`/`md`/`lg`), `iconName`, `iconPosition` (`before`/`after`)                                                                                                                                                          |
| `simple-text`      | Paragraph with optional bold, italic, and links.                                                       | `text`, `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                                          |
| `social-links`     | Row of social media icon links.                                                                        | `links[]`, `variant` (`ghost`/`solid`), `size` (`sm`/`md`), `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                                                                                                                    |
| `spacer`           | Sets the exact gap between two blocks, replacing the automatic spacing.                                | `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                        |
| `text`             | Markdown text with headings, lists, and links.                                                         | `text` (markdown), `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                               |
| `video`            | Video from YouTube, Vimeo, or a local file.                                                            | `type` (`youtube`/`vimeo`/`local-source`), `videoId`, `title`, `source`, `autoplay`, `loop`, `thumbnail`                                                                                                                                                                                             |

### Wrappers — `building-blocks/wrappers/<slug>`

| `<slug>`           | Use for                                                        | Key content props                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `accordion`        | Expandable panels that show or hide content.                   | `label`, `items[]`, `openFirst`, `singleOpen`                                                                                                                                                                                                                                                                                                                                                                   |
| `button-group`     | Arranges buttons in a row or column with even spacing.         | `label`, `buttonSections[]`, `direction` (`row`/`column`), `alignmentHorizontal` (`start`/`center`/`end`)                                                                                                                                                                                                                                                                                                       |
| `card-grid`        | Grid of cards with a cover image and freeform content.         | `label`, `layout` (`grid`/`masonry`), `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `aspectRatio` (`landscape`/`widescreen`/`square`/`portrait`/`none`), `items[]`                                                                                                                                                                                                                            |
| `card`             | Groups related content in a card.                              | `label`, `contentSections[]`, `link`, `rounded`, `border`, `showBeforeAfter`, `beforeContentSections[]`, `afterContentSections[]`                                                                                                                                                                                                                                                                               |
| `content-selector` | Tabs that switch between content panels.                       | `label`, `items[]`, `navigationPosition` (`start`/`top`), `variant` (`plain`/`panel`)                                                                                                                                                                                                                                                                                                                           |
| `grid`             | Many items of the same kind, in as many columns as fit.        | `label`, `columns` (`auto`/`2`/`3`/`4`/`5`/`6`), `minItemWidth`, `maxItemWidth`, `items[]`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                                 |
| `masonry`          | Column layout for mixed-height items that keeps reading order. | `label`, `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `items[]`                                                                                                                                                                                                                                                                                                                              |
| `split`            | Two columns of content side by side.                           | `label`, `firstColumnContentSections[]`, `secondColumnContentSections[]`, `distributionMode` (`quarter-three-quarters`/`third-two-thirds`/`half`/`two-thirds-third`/`three-quarters-quarter`/`fixed-flexible`/`flexible-fixed`), `fixedWidth`, `minSplitWidth`, `alignmentVertical` (`top`/`center`/`bottom`/`stretch`), `reverse`, `reverseOrderOnMobile`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`) |
| `steps`            | Numbered how-it-works sequence with optional images.           | `label`, `orientation` (`vertical`/`horizontal`), `imageAspectRatio` (`landscape`/`portrait`/`square`/`widescreen`), `items[]`                                                                                                                                                                                                                                                                                  |

### Forms — `building-blocks/forms/<slug>`

| `<slug>`       | Use for                                             | Key content props                                                                                                                                                                                                                                                          |
| -------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `choice-group` | Checkboxes or radio buttons for choosing options.   | `title`, `name`, `required`, `options[]`, `orientation` (`vertical`/`horizontal`), `multiple`                                                                                                                                                                              |
| `date`         | Form field for choosing a date.                     | `label`, `name`, `required`, `value`, `min`, `max`, `hint`, `autocomplete`                                                                                                                                                                                                 |
| `file-upload`  | Form field for uploading files.                     | `label`, `name`, `required`, `accept`, `multiple`, `hint`                                                                                                                                                                                                                  |
| `form`         | Groups form fields into one submission.             | `action`, `method` (`post`/`get`), `honeypotName`, `successMessage`, `formBlocks[]`                                                                                                                                                                                        |
| `hidden`       | Hidden form field for extra data.                   | `name`, `value`                                                                                                                                                                                                                                                            |
| `input`        | Single-line text field.                             | `label`, `name`, `type` (`text`/`email`/`password`/`tel`/`url`/`number`), `placeholder`, `required`, `value`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`), `hint`, `autocomplete` |
| `range`        | Slider for choosing a number in a range.            | `label`, `name`, `min`, `max`, `step`, `value`, `unit`, `required`, `showValue`, `hint`, `tickStyle` (`uniform`/`nearest`/`none`), `tickCount`                                                                                                                             |
| `segments`     | Connected buttons for choosing one or more options. | `title`, `name`, `required`, `options[]`, `iconOnly`, `multiple`, `keepStateOnRefresh`                                                                                                                                                                                     |
| `select`       | Dropdown for choosing one option.                   | `label`, `name`, `required`, `options[]`, `placeholder`, `value`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `hint`, `autocomplete`                                                                                |
| `submit`       | Button that submits a form.                         | `text`, `variant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `size` (`sm`/`md`/`lg`), `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`), `hideText`, `disabled`                 |
| `textarea`     | Form field for longer text.                         | `label`, `name`, `required`, `placeholder`, `value`, `hint`, `autocomplete`                                                                                                                                                                                                |
| `toggle`       | On/off switch.                                      | `label`, `name`, `checked`, `required`, `value`                                                                                                                                                                                                                            |

<!-- generated:catalog:building-blocks:end -->
